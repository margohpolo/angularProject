import { ManyTypesPipe } from './many-types.pipe';

describe('ManyTypesPipe', () => {
  it('create an instance', () => {
    const pipe = new ManyTypesPipe();
    expect(pipe).toBeTruthy();
  });
});
